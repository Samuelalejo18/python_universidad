#Separar cadenas (split)

cadena="hola mundo"
lista_cadena=cadena.split()# por default separa cada elemento por espacios en blanco
print(lista_cadena)
datos = "Samuel,20,Colombia"
lista_datos = datos.split(",")
print(f"lista de datos: {lista_datos}")
